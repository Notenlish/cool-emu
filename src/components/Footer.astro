---
import { Button } from "$lib/components/ui/button";
import { footerNavigationLinks } from "$lib/data/navigation";
import Image from "astro/components/Image.astro";
import HeartIcon from "$assets/heart.svg";
---

<footer class="w-full py-4 mt-12 border-t px-2 sm:px-0">
    <div class="container mx-auto flex justify-between items-center">
        <a
            href="/"
            class="gap-2 items-center hidden sm:flex"
            style="font-family: 'Jersey 10'; font-weight:300;"
        >
            <!-- I can't use the <Image /> element because the image optimization & resizing by astro is not helpful as I need the "pixelated" look -->
            <img
                class="h-[51px]"
                style={"aspect-ratio:1/1;image-rendering: pixelated;"}
                alt="Cool Emu"
                src="/ce-icon.svg"
            />
            <span class="text-5xl hidden lg:inline-block">Cool Emu</span>
        </a>
        <nav class="flex gap-6 sm:text-lg text-base h-fit">
            {
                footerNavigationLinks.map((e) => {
                    return (
                        <a class="opacity-70 hover:opacity-100" href={e.url}>
                            {e.text}
                        </a>
                    );
                })
            }
        </nav>
        <div>
            <Button
                href="https://buymeacoffee.com/notenlish"
                variant="link"
                target="_blank"
                class="bg-rose-500 hover:bg-rose-300 text-white py-4 px-4 sm:py-6 sm:px-6 flex align-center"
            >
                <div class="relative">
                    <Image
                        src={HeartIcon}
                        class="opacity-70 animate-pulse"
                        alt="heart"
                        height={24}
                    />
                </div>
                Donate
            </Button>
        </div>
    </div>
</footer>
